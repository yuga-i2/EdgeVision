This folder is intended to hold model weights and exported artifacts.

Place your PyTorch weights (e.g. `yolov8n.pt`) here. Use `src/export_onnx.py` to export an ONNX file.

Large binary files are not included in this repo by default.
